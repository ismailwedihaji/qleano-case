{
  "name": "qleano-case-api",
  "version": "1.0.0",
  "private": true,
  "description": "Tekniskt case: Express-API med medvetna buggar",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "logs": "tail -n 50 -F logs/app.log",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18.17"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "supertest": "^7.1.1"
  }
}
